/** Inline `ewo.` wordmark — bold with the "w" in lime. */
export default function Wordmark() {
  return (
    <span className="font-bold">
      e<span className="text-lime">w</span>o.
    </span>
  );
}
